using UnityEngine;

public class FriendlyStructures : MonoBehaviour
{
	//the maximum structures available on game start (for now its 3, this will prolly change)
	public int maxStructures = 0;
	public int currentStructures = 0;
	
	public void StructureStatus()
	{
	if(currentStructures <+ 0)
	{
		//initialize speedup sequence and other stuff
	}	
	}
}
